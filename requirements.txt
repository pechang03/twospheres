# Core dependencies
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
networkx>=3.0         # Graph analysis for brain connectivity networks
numpy-quaternion>=2022.4.3  # Quaternion rotations for sphere mapping

# Optical simulation
pyoptools>=0.3.7

# MCP server
mcp>=1.0.0
httpx>=0.24.0
uvicorn>=0.22.0
starlette>=0.27.0

# Jupyter for examples
jupyterlab>=4.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional: OpenAI integration
openai>=1.0.0

# Spectroscopy and photon counting (PHY.1.2-packages integration)
lmfit>=1.2.0           # Non-linear least squares with error propagation
emcee>=3.1.0           # MCMC for Bayesian parameter estimation
corner>=2.2.0          # Posterior visualization for emcee
PyDynamic>=2.0.0       # Dynamic metrology with uncertainty propagation

# Optional spectroscopy tools
specutils>=1.12.0      # Spectroscopic data handling
rampy>=0.4.0           # Raman/fluorescence spectroscopy toolkit
